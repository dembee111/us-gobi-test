import React from 'react';
import CollectionPage from '../customTemplates/CollectionPage/CollectionPage';

const TestPage = (props) => (
  <div>
    <CollectionPage
      pageContext={{
        handle: '',
      }}
    />
  </div>
);

export default TestPage;
